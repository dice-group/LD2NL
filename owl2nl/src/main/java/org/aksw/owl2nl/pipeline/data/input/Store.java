package org.aksw.owl2nl.pipeline.data.input;

/**
 * TODO: Since we have to upload the input files, we would try not to upload the same file twice.
 * Thus we handle something like a cache map with IRIs to files.
 *
 */
public class Store {

  // TODO: download a requested model from the hobbit server

  /**
   * <code>
       LOG.info("ONTO ID: " + owlOntology.getOntologyID());
    // OntologyID(OntologyIRI(<http://protege.stanford.edu/plugins/owl/owl-library/koala.owl>)
    // VersionIRI(<null>))
   </code>
   */
}
